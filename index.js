const GetGenerateQrCode = () =>{    const Text_ = document.getElementById('qur_text')    const Validate_Input = () =>{        let IsValid = true;        if(!Text_.value.trim()){            document.getElementById('Error_m').innerText = 'Please Enter Your Text';            Text_.classList.add('is-invalid')            IsValid = false        }else {            Text_.classList.remove('is-invalid')        }        return IsValid    }    document.getElementById('Generate_btn').addEventListener('click',() =>{        // cal the validation form        if(!Validate_Input()){            return        }        // making ajax request        $.ajax({            url: './GenerateQrcode.php',            method: 'post',            data: {Text: Text_.value},            success: function (data,status){                    document.getElementById('Generate_btn').innerText = data;            }        })    })}GetGenerateQrCode()